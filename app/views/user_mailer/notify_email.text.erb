Good day, <%= @user.full_name %>
===============================================

Your account is <%= @is_deleted ? "deleted" : "set inactive" %> by the admin.
You can no longer log into the application afterward.
